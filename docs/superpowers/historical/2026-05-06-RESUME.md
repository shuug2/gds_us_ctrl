# RESUME — Stage A LCD I/O Bring-up 완료 (Task 1-13)

> **시작 시각**: 2026-05-05 (brainstorming + spec + plan)
> **종료 시각**: 2026-05-06 (Task 12 부분 PASS + Task 13 doc sync)
> **결과**: ✅ **PASS (조건부)** — wire-level DGUS LCD 통신 + 1Hz cadence + fault 미히트 검증. visible 페이지 변경은 Stage B 의존 (samd20 ref 의 application 데이터 사전 셋업, scope 정정으로 분리).
> **다음 슬라이스 후보**: (i) Stage B — LCD application 데이터 셋업 (samd20 `init_lcd_mode` 흐름: model_str + DISP_*/LV_*/ICON_* VP 11+ 사전 셋업), (ii) Stage A I/O — CON_OVLD 출력 / CON_START 입력 / CTRL_OSC0~4 GPIO. 별도 spec/plan 필요.

---

## 1. 위치 / 브랜치

| 항목 | 값 |
|------|-----|
| **Worktree** | `/Users/tknoh/dev/work/gds_us_ctrl-stageA/` |
| **Branch** | `feat/stage-a-lcd-io` |
| **Base** | `main @ b8afe1c` (Phase 1+2 merge) |
| **Tip (archive 시점)** | 본 commit |
| **Ahead of main** | ~33 commits (4 doc 초기 + 10 code + 18 doc 정정/RESUME 갱신 + 1 fix + 1 chore + 5 archive cycle) |

---

## 2. 산출 문서

| 파일 | 역할 |
|------|------|
| `docs/superpowers/specs/2026-05-05-stage-a-lcd-io-design.md` | 6 섹션 통합 설계 명세 (정정 5 commits 후 최종) |
| `docs/superpowers/plans/2026-05-05-stage-a-lcd-io.md` | 13 task 구현 계획 (정정 4 commits 후 최종) |
| `docs/superpowers/historical/2026-05-06-RESUME.md` | **본 파일** |

---

## 3. brainstorming 결정 이력 (Q1~Q5 + 레이어링)

| # | 항목 | 결정 |
|---|------|------|
| Q1 | 슬라이스 데모 범위 | (c) init + reset(옵션) + set_page + 1Hz uptime VP write + passive RX |
| Q2 | DGUS HMI 매핑 | (α) samd20 HMI VP 맵 그대로 재사용 (LCD_RUN_STD=9, VAR_POWER=0x1110) |
| Q3 | I/O 전략 | (i) 폴링 TX + IT byte-RX (samd20 패턴 STM32 HAL 미러) |
| Q4 | API 표면 | (C) samd20 9 함수 풀 패리티 |
| Q5 | HW 가용성 | (I) LCD 결선 완료 + samd20 HMI 플래시 가정 |
| Arch | 드라이버 레이어링 | (B) 2층 분리 (`usart1.c` raw + `dgus_lcd.c` 프로토콜) |

---

## 4. 13 Task 진행 상태 (모두 완료)

| # | Task | 리뷰 정책 | Commit | 결과 |
|---|------|----------|--------|------|
| 1 | `fw/include/usart1.h` | controller-direct (mechanical) | `3f3a6e8` | ✅ |
| 2 | `fw/include/dgus_lcd.h` (samd20 포팅) | controller-direct | `20cdffa` | ✅ |
| 3 | `periph.{c,h}` huart1 단일 정의 | controller-direct | `58aac2a` | ✅ |
| 4 | `fw/drivers/usart1.c` raw 드라이버 | spec reviewer ✅ (13/13) | `286d908` | ✅ |
| 5 | `fw/src/irq.c` USART1_IRQHandler | controller-direct | `1701338` | ✅ |
| 6 | `fw/drivers/dgus_lcd.c` skeleton + 카운터 | spec reviewer (Task 6+7 묶음) | `6ebe8f9` | ✅ |
| 7 | dgus_lcd.c TX 빌더 9개 | spec reviewer ✅ (Task 6+7 묶음 20/20) | `4d8baa2` | ✅ |
| 8 | dgus_lcd.c RX 파서 상태머신 | spec reviewer ✅ (12/12) | `e330226` | ✅ |
| 9 | `fw/src/main.c` init 시퀀스 | spec reviewer ✅ (10/10) | `15aa74c` | ✅ |
| 10 | `fw/src/app.c` banner + cadence | spec reviewer ✅ (12/12) | `2d6f20d` | ✅ |
| Q | Bundled code quality reviewer (Task 4-10) | code-reviewer agent | `d8d413c` (HIGH fix) + `48377de` (LOW fix) | ✅ |
| 11 | Build verify (FLASH/RAM/symbol nm) | controller-direct | (산출 ✗ — verify only) | ✅ PASS |
| 12 | HW verify (banner / 페이지 / VP / RX / drop) | controller-direct + 사용자 시각 | (산출 ✗ — verify only) | ⚠️ 부분 PASS (scope 정정) |
| 13 | Doc sync + RESUME archive | controller-direct | `4598bdd` (spec) + `01eef68` (plan) + `34b9837` (changelog) + `b7c5fb8` (pinmap) + 본 commit (archive) | ✅ |

---

## 5. spec/plan reviewer 결과 (모든 substantive Task)

- **Task 4** (`286d908`, `usart1.c` 107 LOC): 13/13 ✅ APPROVE. drift 0. CRITICAL/HIGH/MEDIUM/LOW 0.
- **Task 6+7 묶음** (`6ebe8f9` + `4d8baa2`, dgus_lcd.c skeleton + TX 빌더 9개): 20/20 ✅. samd20 ref 9 함수 풀 패리티 의미적·payload 형식 동등성 확인. severity 0.
- **Task 8** (`e330226`, RX 파서 88 LOC): 12/12 ✅. 4 상태 전이, LEN bound, 50ms 벽시계 timeout, frame_buf 매핑, vp_addr BE, payload 이중 가드 모두 spec 일치. PS_COLLECTING 연산 순서 (timeout vs store) 의 의사코드 sketch 와 코드 차이는 의미적 동등 판정. CRITICAL/HIGH/MEDIUM 0, LOW 1 (RESUME 안내문 잔여 typo, 즉시 정정).
- **Task 9** (`15aa74c`, main.c init +5 LOC): 10/10 ✅. spec §4.1 init 순서 verbatim, usart1_init/dgus_init 위치, race-free 분석, Phase 1+2 init call drift 0, sys_tick_init/__enable_irq explicit 부재, app.c 미수정. severity 0.
- **Task 10** (`2d6f20d`, app.c banner + 1Hz cadence +28 LOC net): 12/12 ✅. §1.3 콜그래프 / §4.2 banner / §4.5 app_init / §4.4 app_loop_iter / §4.6 부팅 출력 표 byte-by-byte 모두 일치. DGUS_DEMO_* 매크로 (하드코딩 ✗), sys_tick_get_ms 사용, mon_writeln/mon_printf 만, Phase 2 hello 라인 보존 + uptime 부착. severity 0, LOW 1 (spec §4.2 line 370 괄호 부연 mismatch — 즉시 정정 `f591635`).

---

## 6. 발견된 spec/plan 결함 + 정정 내역

본 슬라이스에서 4건의 spec/plan 결함을 발견 + 정정. 모두 **코드 first-time commit 전에 spec → plan → code 순으로 정정 commit 처리** (Phase 2 의 `de0c35f` AUTORELOAD typo / `8d67a7d` genex 패턴 미러).

### 6.1 Task 8 — `sys_tick_ms` typo

- spec §3.4/§4.4 + plan Task 8/Task 10 의 함수명 typo: `sys_tick_ms` → 실제는 `sys_tick_get_ms` (Phase 1+2 source of truth).
- 정정: spec(`3e60675`) + plan(`7adede8`) + RESUME followup(`20354f0`). 코드는 정정된 함수명으로 first-time commit (`e330226`).

### 6.2 Task 9 — `sys_tick_init()` / `__enable_irq()` Phase 2 reality 불일치

- spec §1.3 콜그래프 + §4.1 init order + 근거 bullet 의 explicit `sys_tick_init()` / `__enable_irq()` 호출 단정이 Phase 2 reality 와 불일치 (실제 Phase 2 는 sys_tick_init 을 app_init 내부에서 호출, explicit `__enable_irq()` 부재 — Cortex-M PRIMASK 리셋 직후 clear 디시플린).
- 정정: spec(`4353f19`) + plan(`85d11d9`) → 코드(`15aa74c`). spec §4.1 근거 bullet 도 race 분석 (HAL_UART_Receive_IT 시점 ↔ dgus_init 시점) 으로 재작성.
- 배운 점: spec 의 "Phase X 디시플린" 단정 라인은 실제 코드와 cross-check 필수 — typo 보다 큰 구조 drift 가 잠복 가능.

### 6.3 Task 10 — `mon_puts` Phase 2 API 불일치

- spec §4.5 + §1.3 의 `mon_puts` 가 Phase 2 mon API (`mon_writeln` + `mon_printf` only, `mon_puts` 부재) 와 불일치.
- 정정: spec(`77212af`) + plan(`1b9b6b3`) → 코드(`2d6f20d`). banner 문자열에서 explicit "\r\n" 제거 (mon_writeln 자동 append).
- 추가 LOW: spec §4.2 line 370 의 `(LCD_RUN_STD)` / `(VAR_POWER)` 괄호 부연이 §4.5 / §4.6 / plan / code 와 mismatch — reviewer LOW 권고로 즉시 정정 (`f591635`).

### 6.4 Task 12 — visible 페이지 변경 가정 오류 (구조적 scope drift)

- spec §6.3 6c "LCD 페이지 시각 전환" acceptance 가 samd20 ref 의 `init_lcd_mode` 흐름 (`send_model_str` + DISP_*/LV_*/ICON_* VP 11+ 사전 셋업 + 1초 delay + set_page) 의존을 누락. samd20 코드 직접 분석으로 확인.
- 우리 `dgus_set_page(LCD_RUN_STD)` 는 wire 측 정확한 byte sequence (`5A A5 07 82 00 84 5A 01 00 09`) 송신 → LCD 가 echo `5A A5 03 82 4F 4B` ("OK") 응답 → 명령 valid 인식. 그러나 visible 페이지 변화는 application 데이터 부재로 LCD HMI 가 디폴트 페이지 유지 (사용자 보고: "samd20 시절과 동일한 디폴트 페이지").
- 정정: spec §0.3 OUT OF SCOPE 에 "LCD application 데이터 사전 셋업 — Stage B" 명시 추가, §6.3 6c/6d/6f acceptance 를 wire-level 입증 + mon-side cadence + drop counter 정밀 정의 로 완화 (`4598bdd`). plan Task 12 Step 3/4/6 동기화 (`01eef68`).
- 배운 점: brainstorming Q5-(I) "samd20 HMI 플래시 가정" 만으로는 부족 — HMI 가 페이지 visualizer 로 작동하기 위한 application 데이터 의존성도 spec scope 에 명시 필요.

---

## 7. Code quality reviewer 결과 (Task 4-10 묶음)

`code-reviewer` agent dispatch — RESUME §4.5 (갱신 정책) 의 "Task 11 build verify 직전" 시점.

- 검토 영역: 스타일/일관성 (A), 에러 처리/신뢰성 (B), C 표준/컴파일러 안전 (C), HW/RTOS 없는 펌웨어 특화 (D), Phase 1+2 디시플린 보존 (E), 보안 (F).
- **종합 판정**: REQUEST_CHANGES_BUT_HW_VERIFY_OK
- **CRITICAL**: 0 / **HIGH**: 1 / **MEDIUM**: 0 / **LOW**: 3

### 7.1 HIGH 1 — `HAL_UART_ErrorCallback` 미구현 (`d8d413c` fix)

vendor `stm32f4xx_hal_uart.c` 의 `HAL_UART_IRQHandler` 가 USART SR 의 ORE/FE/NE/PE 발견 시 `UART_EndRxTransfer` (RXNEIE clear + state READY) 후 `HAL_UART_ErrorCallback` 만 호출. 이 경로에서는 `HAL_UART_RxCpltCallback` 미발화 → 기존 재무장 코드가 발화 안 함 → ORE 한 번에 RX 영구 정지. samd20 결함 #5 ring overflow 와 별개의 새로운 실패 모드.

Fix: ErrorCallback override 추가, `s_rx_error_count` 카운터 신설, ORE/FE/NE/PE 플래그 클리어 + Receive_IT 재무장.

### 7.2 LOW 3 — Phase 2 디시플린 일관성 (`48377de` fix 일괄)

1. 헤더 가드: `usart1.h` / `dgus_lcd.h` 의 `#ifndef GDS_X_H_` → `#pragma once` (Phase 2 패턴 미러)
2. `irq.c` mid-file `#include` → 파일 상단 이동
3. `usart1.c` 수동 `extern Error_Handler` → `#include "clock.h"` (Phase 2 `usart.c` / `tim.c` 패턴 미러)

### 7.3 Strengths (reviewer 인용)

> samd20 결함 5건 (LEN 무경계 → buffer overflow, byte-counted timeout, init 무한 retry, shared TX race, ring wrap-overwrite) 을 각각 LEN 범위 검증, 벽시계 ms timeout, Error_Handler 즉시 진입, 폴링 TX 자연 직렬화, drop-not-overwrite 정책으로 명시적으로 회피하고 모든 콜백과 send_frame 경로에 결함 번호 주석을 남긴 점이 두드러집니다. 단일 정의 디시플린 (huart1 in periph.c only) + 드라이버 자기 GPIO 책임 원칙이 Phase 2 아키텍처 규율을 완전히 보존하고 있으며, Phase 2 vendor read-only 제약도 git log 로 확인된 0건 변경으로 지켜졌습니다.

---

## 8. Build verify 결과 (Task 11)

| 항목 | 측정 | 기준 (spec §6.2) | 판정 |
|------|------|------------------|------|
| 빌드 | ninja 30/30 → ld OK | exit=0 | ✅ |
| 경고 | warning 0 (newlib-nano stub `_close/_read/_write` 정보 메시지만) | 0 | ✅ |
| FLASH | **24,824 B / 128 KB (18.94%)** | < 30 KB | ✅ (Phase 2 22,060 B → +2,764 B, 예상 +5-8 KB 보다 절약) |
| RAM | **2,744 B / 32 KB (8.37%)** | < 4 KB | ✅ (Phase 2 2,520 B → +224 B, 예상 +200 B 부근) |
| 산출물 | elf (920 KB) / bin (24 KB) / hex (70 KB) / map (505 KB) | 4개 정상 | ✅ |
| 심볼 export (`.text`) | `usart1_init` @ 0x08003a60, `dgus_init` @ 0x0800384c, `dgus_set_page` @ 0x0800388c, `dgus_write_u16` @ 0x080038b8, `dgus_rx_poll` @ 0x080038d6, `USART1_IRQHandler` @ 0x08003eb0, `HAL_UART_RxCpltCallback` @ 0x08003b84, `HAL_UART_ErrorCallback` @ 0x08003c00, `app_init` @ 0x08003c7c, `app_loop_iter` @ 0x08003cb4 | 7개 필수 + 보너스 | ✅ |

미사용 accessor (`dgus_reset_lcd`, `dgus_write_u16_array`, `dgus_rx_drop_count`, `usart1_rx_error_count` 등) 는 `--gc-sections` 으로 링커가 제거. Task 12 의 GDB 직접 메모리 read 로 카운터 관측 (BSS 주소).

---

## 9. HW verify 결과 (Task 12 — 부분 PASS, scope 정정)

ST-LINK V3 + STM32F410RBT 보드 + DGUS LCD (samd20 시절 동일 wiring) flash 후 검증.

### 9.1 acceptance 요약

| # | 단계 | 결과 |
|---|------|------|
| 6a | Flash | ✅ `Programming Finished` + `Verified OK` + `Resetting Target`. STLINK V3J15M7B5S1, target voltage 3.28V. |
| 6b | mon banner | ✅ 사용자 보고: "모니터 출력은 정상" — 3줄 banner + 1Hz `[t=N ms] hello uptime=N` 정상 출력. |
| 6c | LCD wire-level 명령 valid 인식 (정정된 spec) | ✅ — `s_rx_ring` 에 DGUS WR-echo `5A A5 03 82 4F 4B` ("OK") 1Hz 반복 적재. AFRH=0x770 (PA9/PA10 AF7 ✓), BRR=0x341 (115246 baud ✓), CR1=0x202C (UE+RXNEIE+TE+RE ✓), CR3=0x1 (EIE ✓). |
| 6d | mon-side uptime cadence (정정된 spec) | ✅ — mon 1Hz hello uptime=1, 2, 3, ... |
| 6e | RX 터치 이벤트 | ⏸️ 미시도 (사용자 작업 마무리 결정으로 LCD 터치 시도 안 함). HMI 의존 영역. |
| 6f | drop counter (정정된 spec) | ✅ — `usart1_rx_drop_count = 0`, `usart1_rx_error_count = 0`, `dgus_tx_timeout_count = 0`. `dgus_rx_drop_count` ≈ 운영시간 (sec) 으로 echo 누적분 일치 (LEN=3 echo 가 `DGUS_LEN_MIN=4` 으로 의도 drop). 다른 사유 drop 부재. |
| 6g | no fault | ✅ PC=`app_loop_iter`(app.c:45), Thread mode (xPSR 0x61000000 / 0x81000000), CFSR=0, HFSR=0. MSP=0x20007fc8 (스택 사용 ~56 bytes). |

> 첫 openocd halt 에서 일시 HardFault 상태 보였으나 두 번째 halt 시 정상 Thread mode + 모니터 출력 정상 → **첫 halt 의 HardFault 는 openocd reset/halt 시퀀스 안정화 중 일시 상태로 false positive** 판정.

### 9.2 visible 페이지 변경 미관측 (scope 정정 영역)

- 사용자 보고: "LCD 페이지는 바뀌지 않음, samd20 시절과 동일한 디폴트 페이지". samd20 시절엔 동일 명령으로 visible 변화 정상 동작.
- 진단: samd20 ref `set_lcd_page` 의 wire byte sequence (`5A A5 07 82 00 84 5A 01 00 PP`) 와 우리 `dgus_set_page` 가 **100% 동일**. 차이는 boot 시퀀스 — samd20 main 은 `set_lcd_page(0)` → **`delay_ms(1000)`** → `init_lcd_mode()` (안에서 `send_model_str` + 11+ VP 사전 채움 + 마지막 set_page) 호출.
- 결론: LCD HMI 가 visible 페이지를 그리려면 application 데이터 (model 문자열 / DISP_* / LV_* / ICON_*) 가 사전 채워져 있어야 함. Stage A 슬라이스의 set_page 단독 호출 = 명령 valid + LCD 내부 page register 변경 (echo OK) 까지만 입증. visible 변화는 Stage B 의존.
- 처리: spec §0.3 OUT OF SCOPE + §6.3 6c/6d acceptance 정정 (위 §6.4 정정 내역 참조).

---

## 10. 환경 / 알려진 이슈 (Phase 1+2 RESUME 에서 이전 + 본 슬라이스 추가)

### 10.1 `$STM32_TOOLCHAIN` env var stale (Phase 2 §2.2 회귀)

사용자 shell 의 `STM32_TOOLCHAIN=/Users/tknoh/dev/sdk/mcu/stm32/toolchain` 은 존재 ✗. 빌드 시 `env -u STM32_TOOLCHAIN cmake ...` 사용. plan 의 모든 build/verify step 에 이미 반영됨.

### 10.2 Cortex-M4 r0p1 6 HW BP 한계 (Phase 2 §2.5 회귀)

GDB break 등록 갯수 ≤ 6. fault handler 모두 + main 동시 break ✗.

### 10.3 USART1 AF 번호 (R1 — spec §5.3 리스크) ✅ 검증 완료

PA9/PA10 = AF7 — Task 12 GDB dump 로 GPIOA AFRH = 0x00000770 확인. PA9 (bits[7:4]) = 7, PA10 (bits[11:8]) = 7. ✅

### 10.4 clangd LSP 경고 (IDE 노이즈)

editor 의 clangd 진단이 `stm32f4xx_hal.h` / `uint8_t` 등 unknown 으로 ✗ 표기 — IDE include path 미설정. 검증 기준은 `arm-none-eabi-gcc -fsyntax-only` 결과 (Stage A 모든 파일 exit=0, warning 0). clangd 노이즈 무시.

### 10.5 corpus 임계 + graphify (본 슬라이스 추가)

vendor 포함 corpus 가 3M 단어 / 280 파일 (88% 가 fw/vendor/ ST HAL) — graphify 임계 초과. 이 슬라이스에서 두 번 모두 사용자가 graphify 스킵 채택. 사용자 메모리 `feedback_graphify_after_docs` 정책 자체는 유지하되 매 세션 강제 아님. 다음 슬라이스 시작 시 동일 결정 필요.

### 10.6 1M context 환경 인사이트 (본 슬라이스 추가)

Opus 4.7 1M context 환경에선 substantive Task 묶음 (Task 9 + 10) 처리 후에도 ~17% 만 소비. 표준 200k context 기준 50% 임계 정책은 다른 모델 세션에서만 의미. `/context` 명령으로 정기 점검.

---

## 11. 다음 슬라이스 권고

본 RESUME 갱신 시점에 **3 가지 후속 슬라이스 후보**:

### 11.1 Stage B — LCD application 데이터 사전 셋업 (visible 페이지 변경 보장)

- 목표: samd20 ref 의 `init_lcd_mode` 흐름 포팅
  - `send_model_str(model_freq, model_type)` — "GDS-15/20/30..." 모델 문자열을 LCD 의 model 영역에 송신
  - 11+ VP write: `DISP_ENERGY_EN`, `DISP_MULTI_EN`, `LV_DM_DELAY/WELD/HOLD`, `LV_TM_WELD/HOLD`, `LV_WORK_CNT`, `LV_ENERGY_EDIT`, `ICON_RESET/SEEK/RUN`, `DISP_HORNDOWN`
  - boot 시퀀스: `dgus_set_page(0)` → `delay_ms(1000)` → 위 VP 들 사전 셋업 → `dgus_set_page(LCD_RUN_STD)`
- 산출 spec/plan 별도. brainstorming + spec self-review + plan 단계 재 거침.
- 예상 LOC: app.c 확장 +50, 새 driver helper 또는 dgus_lcd.c 확장 +20.
- 검증: visible 페이지 9 (LCD_RUN_STD) 표시 ✅ + VAR_POWER 시각 1 Hz tick ✅ — 본 슬라이스의 spec §6.3 6c/6d 의 원래 의도였던 시각 검증 완성.

### 11.2 Stage A I/O — CON_*/CTRL_OSC 등 GPIO

- 목표: 외부 출력 (CON_OVLD = PB3 — 현재 Phase 2 heartbeat 임시 점유) + 입력 (CON_START 등) + CTRL_OSC0~4 GPIO 활성화.
- 본 슬라이스의 §0.3 OUT OF SCOPE 에 명시된 영역.
- 산출 spec/plan 별도.

### 11.3 Stage C — Modbus RTU on USART6 (Phase 2 mon 과 이중 역할)

- 목표: USART6 의 Modbus RTU 모드 전환 메커니즘 + 프로토콜 layer.
- pinmap 의 PC6/PC7 가 "MON_TX (Phase 2) / MB_TX (Stage B 이후)" 이중 역할로 명시됨.
- 산출 spec/plan 별도. brainstorming 단계 + Modbus 라이브러리 선택 결정 필요.

권장 우선순위: **Stage B → Stage A I/O → Stage C** (Stage B 가 본 슬라이스의 자연 후속, visible 페이지 검증 완성).

---

## 12. 결정 / 합의 이력 (요약)

| 결정 | 값 |
|------|-----|
| Stage A 슬라이스 범위 | LCD-only first slice (init + reset옵션 + set_page + 1Hz uptime + passive RX) |
| 통신 | USART1 PA9/PA10 AF7, 115200 8N1, 폴링 TX + IT 1-byte RX |
| 레이어링 | `usart1.c` raw + `dgus_lcd.c` 프로토콜 (Phase 2 패턴 미러) |
| HW 검증 | LCD 결선 + samd20 HMI 가정. Phase 2 chunk 12 동등 검증 |
| 응답 언어 | 한국어 (memory `feedback_korean_responses`) |
| Task 1-7 spec drift | 없음 |
| Task 8 spec drift | `sys_tick_ms` → `sys_tick_get_ms` (3 commit). reviewer 12/12 ✅ |
| Task 9 spec drift | spec §1.3/§4.1 explicit `sys_tick_init()` / `__enable_irq()` 단정 제거. reviewer 10/10 ✅ |
| Task 10 spec drift | spec §4.5/§1.3 `mon_puts` → `mon_writeln` + §4.2 line 370 괄호 부연 정정. reviewer 12/12 ✅ |
| Task 12 scope drift | visible 페이지 변경 가정 오류 → §0.3 OUT OF SCOPE 보강 + §6.3 acceptance 완화. Stage B 분리 |
| Code quality reviewer | HIGH 1 (HAL_UART_ErrorCallback) + LOW 3 (Phase 2 디시플린) 모두 fix |
| HW verify 결과 | 부분 PASS (조건부) — wire/cadence/fault 검증 ✅, visible 페이지 = Stage B 의존 |
| 본 RESUME 운명 | 본 파일 = archive. `docs/superpowers/RESUME.md` 는 다음 슬라이스 진입 시 새로 작성 |

---

## 13. 트러블슈팅 메모 (Phase 2 §6 + 본 슬라이스 추가)

### "빌드 실패: arm-none-eabi-gcc not found"
§10.1 STM32_TOOLCHAIN 이슈. `env -u STM32_TOOLCHAIN ...` 사용.

### "feat/stage-a-lcd-io 브랜치가 없다"
Worktree 사라짐. `cd /Users/tknoh/dev/work/gds_us_ctrl && git worktree add ../gds_us_ctrl-stageA feat/stage-a-lcd-io` 로 복원.

### "Cannot insert hardware breakpoint N"
§10.2 — Cortex-M4 r0p1 6 HW BP 한계. break 갯수 줄여서 재시도.

### "spec 결함 발견 시"
Task 8/9/10/12 의 정정 패턴 미러: spec → plan → code 순서로 별도 commit. 코드 first-time commit 직전.

### "openocd 첫 halt 시 HardFault 보임"
일시 false positive 가능 (안정화 중 상태). 두 번째 halt 시 Thread mode + 모니터 정상 출력 으로 cross-check. 진짜 fault 라면 모니터 출력 멈춤.

### "LCD 가 echo OK 까지 응답하는데 visible 페이지 변화 ✗"
Stage A scope 영역 — application 데이터 사전 셋업 부재. Stage B 진입 필요. 본 RESUME §6.4 + §11.1 참조.

---

> **Phase 1+2 RESUME** archive: `docs/superpowers/historical/2026-05-05-RESUME.md` (이전 슬라이스 종료 시점)
> **Stage A 직접 archive 시점**: 2026-05-06
